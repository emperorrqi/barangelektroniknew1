## Path
cd C:\Users\PLN\manajemengudang-mysql

## cetak
buatkan versi final entry_barang_pesanan.php lengkap dengan tombol Cetak Pesanan via browser tanpa FPDF
cetak basterima seperti entry pesanan

## 
docker-compose down
docker-compose up --build

## CARA HAPUS sql
docker start manajemengudang-mysql
docker exec -it manajemengudang-mysql mysql -u root -prootpassword
pass..
USE manajemengudang;
DROP DATABASE manajemengudang;

## CARA BALIKIN sql
docker start manajemengudang-mysql
docker exec -it manajemengudang-mysql mysql -u root -prootpassword
Get-Content .\sql\init.sql | docker exec -i manajemengudang-mysql mysql -u root -prootpassword manajemengudang
pass..
CREATE DATABASE manajemengudang;
Use manajemengudang;

##ddd
exit;
docker exec -i manajemengudang-mysql mysql -u root -prootpassword manajemen_gudang < sql/init.sql
docker exec -it manajemengudang-mysql mysql -u root -prootpassword
USE manajemen_gudang;
SHOW TABLES;
drag init.sql

## FS(0)
USE manajemengudang;
DROP DATABASE manajemengudang;

#FS (1)
docker start manajemengudang-mysql
docker exec -it manajemengudang-mysql mysql -u root -prootpassword

CREATE DATABASE manajemengudang;
USE manajemengudang;
exit;

## FS(2) powershell
docker start manajemengudang-mysql
Get-Content sql/init.sql -Raw | docker exec -i manajemengudang-mysql mysql -u root -prootpassword manajemengudang


## FS(2) CMD
docker start manajemengudang-mysql
type sql\init.sql | docker exec -i manajemengudang-mysql mysql -u root -prootpassword manajemengudang


## FS (3)
docker start manajemengudang-mysql
docker exec -it manajemengudang-mysql mysql -u root -prootpassword
USE manajemengudang;
SHOW TABLES;

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/emperorrqi/sistempersediaanbarangelektronik.git
git push -u origin main

git remote add origin https://github.com/emperorrqi/sistempersediaanbarangelektronik.git
git branch -M main
git push -u origin main

https://railway.app/

ihgedBsnysOkDSBNRHKSjKZAhEcWTELj
